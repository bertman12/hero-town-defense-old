/**
 * Randomly creates a set of scenarios that will happen in game and schedules the events with timers.
 */

export class EventScheduler {

    static createEvent(){
        //pick what kind of event to create
    }

    static eventPool; //set of events that can be created, I guess should also be categorized into what kind of events they are

    //Enemy spawn events require a predefined set of enemies to spawn, possibly with some variance modifiers.
    

} 